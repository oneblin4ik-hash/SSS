# Бриф v2 — разворот продукта на «первые шаги»

Читается вместе с `CLAUDE.md`. Тот описывает *как* устроен код, этот — *что*
уже сделано и *куда* поворачиваем. Если открыл новый чат: прочитай оба файла,
дальше можно работать без пересказа истории.

Ветка: `claude/serbolin-pdf-lead-magnet-t69jnf`, PR #7.

---

## 1. Что уже готово

Воронка Эдуарда Серболина в Telegram: **квиз → персональный PDF-разбор →
оффер → трипваер на 14 дней**. Всё в дизайн-системе Crimson.

| Кусок | Где | Состояние |
|---|---|---|
| Квиз (Mini App) | `quiz-test/kviz-serbolin.html` | работает, 14 экранов, демка и тесты рядом |
| Персональный PDF | `serbolin-pdf/build_lead_magnet.py` | собирается на лету из ответов квиза |
| Страница «Перед стартом» | `serbolin-pdf/data/intro.py` | мотивация: привычка / мышление / дисциплина |
| 14 уроков + бланки | `serbolin-pdf/data/days.py`, `build_tripwire.py` | 17 PDF, дни 2/6/10 тёмные |
| Интерактивный курс | `serbolin-pdf/build_course.py` | все 14 уроков одним HTML |
| Страница оффера | `offer-page/build_offer.py` | три слайда, 1 890 ₽, CTA в личку; тем же файлом — PDF |
| Инструкция владельцу | `docs/guide.html` | как собрать это в Telegram-боте |
| ТЗ на бота | `serbolin-pdf/source/bot-integratsiya-Serbolin.md` | продажа в личке, гейт по подписке на канал |

Тексты-первоисточники лежат в `serbolin-pdf/source/*.md` — их и правим,
а не выдумываем заново.

## 2. Что меняем

Продукт был построен вокруг **срыва и возвращения**: почему бросил, где
отвалился, как не откатиться. Разворачиваем на **«не знаю, с чего начать»**.

Рабочее название смысла: **первые шаги к желаемой форме**.

Две аудитории, обе обязательны:

1. **Новичок.** Никогда не занимался. Не знает, с чего начать — худеть,
   тренироваться, что вообще делать в первую неделю.
2. **Возвращенец.** Занимался, бросил, вернулся, снова бросил. Хочет войти
   в тренировочный процесс правильно, а не по третьему кругу.

Что из этого следует:

* **Тест обязан определять, занимался ли человек вообще.** Это новая ось
  сегментации вместо «где точка срыва». Старая логика `typeOf()` в
  `kviz-serbolin.html` (4 типа вокруг срывов) переписывается — и вместе с ней
  один в один `serbolin-pdf/lib/profile.py`, иначе цифры на экране и в PDF
  разъедутся.
* **Суть курса** — с чего правильно начать: похудение, набор массы, первые
  шаги в фитнесе. Не «разбор причин срыва».
* **Тон** — больше мотивации, объяснение простым языком, максимум пользы,
  без воды, в голосе Эдуарда (см. §10 `CLAUDE.md`).
* Формат прежний: **14 дней, 14 уроков**.
* **В оффер добавляются отзывы покупателей с фото.** Пока заглушки —
  владелец разрешил их придумать явно, потом заменит на настоящие. Место под
  них уже размечено в `offer-page/build_offer.py` (блок `.ph`).

## 3. Чего не делаем

* Дашборд (`index.html`, `src/`) — заморожен, владелец сказал «забудь».
* Платёжная система — продажа только в личке `@Mr_Serbolin`.
* Фотографии людей нигде, кроме санкционированных отзывов и `avatar.png`.
* Никаких выдуманных фактов, цифр и рейтингов сверх этих заглушек.

## 4. Проверка перед сдачей

```bash
cd serbolin-pdf && python3 build_tripwire.py && python3 preview.py
cd quiz-test && python3 restyle_quiz.py && python3 build_quiz_demo.py && python3 test_quiz_demo.py
```

Сборка обязана напечатать «Готово, переполнений нет.» и «Внешних ресурсов нет.»

## 5. Решения владельца

Согласовано 21.08.2026. Это ответы, а не варианты — работаем по ним.

### 5.1 Сегментация: две оси, опыт × цель

Тип человека складывается из двух ответов, а не из одного.

**Ось 1 — опыт** (её и определяет новый вопрос «занимался ли вообще»):

| Код | Кто это |
|---|---|
| `never` | никогда не занимался, начинает с нуля |
| `quit` | занимался раньше, была длинная пауза, возвращается |
| `onoff` | занимается урывками годами, результата нет |

**Ось 2 — цель**: похудеть · набрать массу · привести в форму.

Реализация: **название и характер типа даёт опыт, цель подмешивает блок
плана и рекомендаций.** Три текста типов + три блока цели композицией
покрывают все девять сочетаний — писать девять отдельных портретов не нужно
и поддерживать их потом невозможно.

`typeOf()` в `quiz-test/kviz-serbolin.html` и `lib/profile.py` переписываются
синхронно, один в один — иначе экран и PDF разъедутся.

### 5.2 Состав теста

**Остаётся:** пол, возраст, рост/вес (ИМТ), цель, ограничения по здоровью,
шкала фигур «сейчас → хочу».

**Уходит:** «сколько раз начинал» и «на каком дне бросал» — вся ветка про
срывы. Вместо неё вопросы про опыт: занимался ли вообще, когда последний раз,
что именно делал.

**Переформулируется** (было «сколько времени реально есть в день»):

* «Сколько времени готов тратить на тренировку» — варианты, не поле ввода;
* «Сколько раз в неделю готов тренироваться» — отдельный вопрос.

**Добавляется:** акцент на группу мышц. Оговорка, которую держим в голове:
за 14 дней сплита не построить, поэтому ответ работает как акцент в разборе
и как дополнительное движение в тренировке, а не как обещание программы на
конкретную зону. Обещать «курс под ягодицы» нельзя — это неправда.

### 5.3 Название

**«Первые шаги к форме»**, подзаголовок «14 дней — с чего начать и как не
бросить». Меняется в четырёх местах: обложка трипваера, квиз, страница
оффера, ТЗ на бота.

### 5.4 Уровни курса

Переименовать под новичка. Рабочий вариант: 🔍 Точка старта · ⚙️ Первые шаги ·
🧩 Твоя система · 🛡 Уже привычка. Правится в `lib/theme.py` (`level_for_day`),
дальше подхватывается прогресс-баром автоматически.

### 5.5 Уроки

Скелет из 14 тем остаётся — порядок рабочий, вёрстка и бланки выверены.
Переписываются тексты: подача с «разбираем срыв» на «вот с чего начать»,
больше мотивации и объяснений простым языком. День 12 «Протокол срыва»
меняется на урок о том, как не бросить.

### 5.6 Отзывы

Три штуки: фото, имя, возраст, строка результата, 2–3 предложения. По одному
на сценарий — новичок, возвращенец, «занимался урывками». Заглушки рисуются
кодом или генерируются, настоящих людей не берём. Место готово — блок `.ph`
в `offer-page/build_offer.py`.

### 5.7 Питание разведено по цели

**Сделано.** Курс был написан из позиции худеющего: слова «набор», «масса» и
«профицит» не встречались в четырнадцати уроках ни разу, при том что цель
«набрать мышечную массу» в тесте есть. Хуже того, три места говорили
набирающему прямо противоположное — день 3 учил срезать жидкие калории,
день 2 наедаться овощами, день 7 не пугаться неподвижных весов.

Теперь веток две: **дефицит** (`loss`, `tone`) и **профицит** (`mass`).

* **Тексты.** Абзац в спеке помечен `[дефицит]` или `[профицит]`, без метки —
  общий. Расходятся дни 2, 3, 6, 7, 8 и 10, все про питание.
* **Страницы.** Свои версии у дней 3, 6 и 7, имя с суффиксом `-nabor`.
  Остальные одиннадцать общие.
* **Проверка.** Сборка падает, если метка потерялась в дне из `GOAL_DAYS`
  или появилась там, где её никто не покажет.
* Симулятор чата получил переключатель цели, интерактив показывает обе ветки
  рядом с пометкой, кому какая уходит.

Цель «привести в форму» идёт по ветке дефицита с оговоркой в дне 7: там
задача поменять состав, а не цифру на весах.

Тренировки этой правкой не тронуты — они расходятся по месту занятий, а не
по цели, и это следующий шаг.

### 5.7а Пол: физиология, а не только род

Согласование рода в квизе было сделано хорошо (28 вызовов `f(жен, муж)`),
а физиология игнорировалась. Закрыто три места:

* **Темп прогноза** — был общий, женщина получала мужской срок. Теперь
  таблица по полу в `plan()` и `profile.py` синхронно, разницу ловит
  `test_profile_parity.py`.
* **Цикл** — не упоминался нигде, при том что главная аудитория женская, а
  день 7 весь про «весы стоят, не паникуй». Задержка воды даёт до полутора
  килограммов, и женщина бросает ровно здесь. Добавлен абзац под меткой `[ж]`.
* **Подписи под фигурами** в квизе стояли в женском роде для всех — мужчина
  видел «подтянутая» под мужским силуэтом.

Метки в спеке: `[ж]` и `[м]`, ось независимая от цели. Расходится один день,
седьмой, — больше в уроках пол не меняет ничего. Программу тренировок он
разведёт позже, на шагах с видео.

### 5.7б Тест спрашивает, где человек будет заниматься

Новый экран во втором блоке: дома · в зале · ещё не решил. Поле `pl` в
payload, `Profile.place` и признак `at_gym` в Python.

Третий вариант ведёт в домашний курс: начать дома можно сегодня. Убирать его
нельзя — часть людей приходит именно с этим вопросом.

Программа тренировок пока одна на всех, поле только копится. Но копится с
первого дня: когда появятся курсы под дом и под зал, будет видно, кому какой
предлагать и сколько людей вообще звал зал.

### 5.8 Оффер разбит на три слайда

Решение владельца, уже сделано. Было — одна длинная страница, стало:

1. **Обложка.** Кто говорит, что за курс, персональная строка по тесту, три
   числа-факта и мысль, ради которой листают дальше.
2. **Ценность.** Четыре шага курса и одиннадцать инструментов, которые
   остаются после него.
3. **Решение.** Отзывы, цена, кнопка в личку, семь частых вопросов.

Носитель — Mini App: кнопка «Написать Эдуарду» открывает диалог в одно
касание, а FAQ гармошкой занимает семь строк вместо половины слайда. PDF
собирается тем же скриптом и идёт рядом — он остаётся в переписке и его
пересылают. Тексты у обоих одни и те же, расходиться негде.

Страницы PDF разной высоты, и это намеренно: слайды не равны по объёму, а
одна высота на всех означала бы либо резать третий, либо оставить первые
два наполовину пустыми. Подробности — в `offer-page/README.md`.
