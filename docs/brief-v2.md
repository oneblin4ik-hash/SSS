# Бриф v2 — разворот продукта на «первые шаги»

Читается вместе с `CLAUDE.md`. Тот описывает *как* устроен код, этот — *что*
уже сделано и *куда* поворачиваем. Если открыл новый чат: прочитай оба файла,
дальше можно работать без пересказа истории.

Ветка: `claude/serbolin-pdf-lead-magnet-t69jnf`, PR #7.

---

## 1. Что уже готово

Воронка Эдуарда Серболина в Telegram: **квиз → персональный PDF-разбор →
оффер → трипваер на 14 дней**. Всё в дизайн-системе Crimson.

| Кусок | Где | Состояние |
|---|---|---|
| Квиз (Mini App) | `quiz-test/kviz-serbolin.html` | работает, 14 экранов, демка и тесты рядом |
| Персональный PDF | `serbolin-pdf/build_lead_magnet.py` | собирается на лету из ответов квиза |
| Страница «Перед стартом» | `serbolin-pdf/data/intro.py` | мотивация: привычка / мышление / дисциплина |
| 14 уроков + бланки | `serbolin-pdf/data/days.py`, `build_tripwire.py` | 17 PDF, дни 2/6/10 тёмные |
| Интерактивный курс | `serbolin-pdf/build_course.py` | все 14 уроков одним HTML |
| Страница оффера | `offer-page/build_offer.py` | 690 ₽, зачёркнуто 1 990 ₽, CTA в личку |
| Инструкция владельцу | `docs/guide.html` | как собрать это в Telegram-боте |
| ТЗ на бота | `serbolin-pdf/source/bot-integratsiya-Serbolin.md` | продажа в личке, гейт по подписке на канал |

Тексты-первоисточники лежат в `serbolin-pdf/source/*.md` — их и правим,
а не выдумываем заново.

## 2. Что меняем

Продукт был построен вокруг **срыва и возвращения**: почему бросил, где
отвалился, как не откатиться. Разворачиваем на **«не знаю, с чего начать»**.

Рабочее название смысла: **первые шаги к желаемой форме**.

Две аудитории, обе обязательны:

1. **Новичок.** Никогда не занимался. Не знает, с чего начать — худеть,
   тренироваться, что вообще делать в первую неделю.
2. **Возвращенец.** Занимался, бросил, вернулся, снова бросил. Хочет войти
   в тренировочный процесс правильно, а не по третьему кругу.

Что из этого следует:

* **Тест обязан определять, занимался ли человек вообще.** Это новая ось
  сегментации вместо «где точка срыва». Старая логика `typeOf()` в
  `kviz-serbolin.html` (4 типа вокруг срывов) переписывается — и вместе с ней
  один в один `serbolin-pdf/lib/profile.py`, иначе цифры на экране и в PDF
  разъедутся.
* **Суть курса** — с чего правильно начать: похудение, набор массы, первые
  шаги в фитнесе. Не «разбор причин срыва».
* **Тон** — больше мотивации, объяснение простым языком, максимум пользы,
  без воды, в голосе Эдуарда (см. §10 `CLAUDE.md`).
* Формат прежний: **14 дней, 14 уроков**.
* **В оффер добавляются отзывы покупателей с фото.** Пока заглушки —
  владелец разрешил их придумать явно, потом заменит на настоящие. Место под
  них уже размечено в `offer-page/build_offer.py` (блок `.ph`).

## 3. Чего не делаем

* Дашборд (`index.html`, `src/`) — заморожен, владелец сказал «забудь».
* Платёжная система — продажа только в личке `@Mr_Serbolin`.
* Фотографии людей нигде, кроме санкционированных отзывов и `avatar.png`.
* Никаких выдуманных фактов, цифр и рейтингов сверх этих заглушек.

## 4. Проверка перед сдачей

```bash
cd serbolin-pdf && python3 build_tripwire.py && python3 preview.py
cd quiz-test && python3 restyle_quiz.py && python3 build_quiz_demo.py && python3 test_quiz_demo.py
```

Сборка обязана напечатать «Готово, переполнений нет.» и «Внешних ресурсов нет.»

## 5. Открытые вопросы к владельцу

Ответы вписываются сюда же, чтобы новый чат их видел.

1. Сколько типов должен выдавать тест и какие.
2. Что из старого теста остаётся (ИМТ, цель, ограничения по здоровью, окна
   времени) и что уходит.
3. Меняется ли обещание продукта на обложке и в оффере.
4. Сколько отзывов и в каком формате.
5. Остаются ли четыре уровня курса 🔍 Диагностика · ⚙️ Первые шаги ·
   🧩 Система · 🛡 Закрепление или переименовываются под новичка.
