# Промежуточный HTML и растровые превью — воспроизводятся сборкой.
# Готовые PDF в out/ версионируем: их забирает бот.
build/
preview/
# Архив для отправки — та же пачка PDF, что уже лежит рядом распакованной.
out/tripvaer-pdf.zip
__pycache__/
*.pyc
