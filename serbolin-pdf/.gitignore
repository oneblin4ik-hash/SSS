# Промежуточный HTML, растровые превью и рабочие скриншоты — всё
# воспроизводится сборкой. Готовые PDF в out/ версионируем: их забирает бот.
build/
preview/
shots/
shots-*.png
# Архив для отправки — та же пачка PDF, что уже лежит рядом распакованной.
out/tripvaer-pdf.zip
__pycache__/
*.pyc
# Курс одним документом — склейка тех же kurs-*.pdf, что лежат рядом.
# Версионировать 11 МБ дубля незачем, собирается из build_kurs.py.
out/kurs-polnyy.pdf
